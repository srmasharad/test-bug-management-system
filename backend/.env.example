# Database Configuration
# For local development, leave empty to use SQLite
# For production (Neon), set your PostgreSQL connection string
DATABASE_URL=

# Example for Neon:
# DATABASE_URL=postgresql://username:password@your-neon-host.com/dbname?sslmode=require

# Server Configuration
PORT=3000
NODE_ENV=development

# Seed data (set to 'true' to populate test data)
SEED_DATA=true
